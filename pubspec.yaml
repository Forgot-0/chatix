name: chatix
description: "ChatiX - A Flutter application."
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number is used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
# In Windows, build-name is used as the major, minor, and patch parts
# of the product and file versions while build-number is used as the build suffix.
version: 1.0.0+1

environment:
  sdk: '>=3.10.0 <4.0.0'

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter

  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  cupertino_icons: ^1.0.8
  flutter_riverpod: ^3.0.3
  go_router: ^17.0.1
  freezed_annotation: ^3.0.0
  json_annotation: ^4.9.0
  equatable: ^2.0.7
  dio: ^5.8.0+1
  dio_cookie_manager: ^3.1.1
  cookie_jar: ^4.0.8
  flutter_dotenv: ^5.2.1
  path_provider: ^2.1.5
  shared_preferences: ^2.5.3
  hive: ^2.2.3
  flutter_secure_storage: ^10.0.0
  intl: ^0.20.2
  connectivity_plus: ^7.0.0
  cached_network_image: ^3.4.1
  shimmer: ^3.0.0
  url_launcher: ^6.3.1
  package_info_plus: ^9.0.0
  in_app_review: ^2.0.9
  workmanager: ^0.9.0+3
  flutter_tts: ^4.2.3
  uuid: ^4.3.3
  synchronized: ^3.1.0
  local_auth: ^3.0.0
  riverpod_annotation: ^3.0.3
  fpdart: ^1.2.0
  golden_toolkit: ^0.15.0
  web_socket_channel: ^3.0.3
  flutter_form_builder: ^10.2.0
  form_builder_validators: ^11.2.0
  image_picker: ^1.1.2
  # Used directly (not just transitively via dio) for MediaType when
  # building the presigned avatar upload's multipart request — see
  # features/profile/data/datasources/avatar_uploader_impl.dart.
  http_parser: ^4.1.2
  # LiveKit voice/video calls (api-docs §6.6). `POST /chats/{id}/calls/join/`
  # returns a `JoinTokenDTO {token, slug, livekit_url}` which this SDK consumes
  # via `Room.connect(livekitUrl, token)`. Also pulls in `permission_handler`
  # transitively for the mic/camera prompts.
  permission_handler: ^12.0.0
  # Document attachments (api-docs §6.5's `file` bucket: pdf/zip/txt/doc/docx/
  # xlsx, ≤100 MB, max 1 per message). `image_picker` only reaches the
  # image/video bucket, so without this the documented file types are
  # unattachable. Configured with `ChatAttachmentLimits.allowedExtensions` so
  # unsupported files can't be selected in the first place.
  file_picker: ^11.0.3
  livekit_client: ^2.9.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  # The "flutter_lints" package below contains a set of recommended lints to
  # encourage good coding practices. The lint set provided by the package is
  # activated in the `analysis_options.yaml` file located at the root of your
  # package. See that file for information about deactivating specific lint
  # rules and activating additional ones.
  flutter_lints: ^6.0.0
  build_runner: ^2.4.15
  freezed: ^3.0.6
  json_serializable: ^6.9.5
  flutter_gen_runner: ^5.10.0
  change_app_package_name: ^1.5.0
  riverpod_generator: ^3.0.3
  riverpod_lint: '>=3.0.0 <3.1.0'
  custom_lint: '>=0.8.0 <0.9.1'
  mocktail: ^1.0.4
  flutter_launcher_icons: ^0.14.4

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true
  # Required because `l10n.yaml` exists: the tool runs gen_l10n as part of every
  # build and hard-fails ("Attempted to generate localizations code without
  # having the flutter: generate flag turned on") unless this is set — so
  # without it `flutter build` / `flutter run` cannot start at all.
  generate: true

  # Assets for the application
  assets:
    - .env
    # Common assets
    - assets/images/
    
    # Language-specific assets
    - assets/images/en/
    - assets/images/es/
    - assets/images/fr/
    - assets/images/de/
    - assets/images/ja/
    - assets/images/bn/
    
    # Fonts
    - assets/fonts/

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/to/resolution-aware-images

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/to/asset-from-package

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/to/font-from-package